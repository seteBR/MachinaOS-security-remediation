# Dev-mode env overrides — copy to `.env.dev` for per-checkout runtime state.
#
# `machina dev` layers `.env.dev` on top of `.env.template` and `.env`.
# `machina start` / `machina daemon` do not load this file.

# Per-checkout state under <repo>/.machina/.
DATA_DIR=.machina
